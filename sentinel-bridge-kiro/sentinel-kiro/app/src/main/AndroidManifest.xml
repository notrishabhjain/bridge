<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- DownloadManager's provider enforces INTERNET on the calling app, so the
         model download in the setup wizard needs it even though the system
         process performs the transfer. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Package visibility (Android 11+ / targetSdk 30+): without these the
         recorder is invisible to getPackageInfo() and getLaunchIntentForPackage()
         even when it is installed, so the Device Check fails and the pipeline
         cannot launch the Recorder. The MAIN/LAUNCHER intent query covers any
         recorder the user configures at runtime; the explicit entries cover the
         known Xiaomi/AOSP package names. -->
    <queries>
        <package android:name="com.miui.voiceassist" />
        <package android:name="com.android.soundrecorder" />
        <package android:name="com.miui.recorder" />
        <package android:name="com.xiaomi.soundrecorder" />

        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:name=".SentinelApplication"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <!-- Disable default WorkManager initializer so SentinelApplication provides
             a Hilt-aware Configuration via Configuration.Provider. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <activity
            android:name="com.sentinel.bridge.feature.setup.SetupWizardActivity"
            android:exported="true"
            android:label="Sentinel Setup">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="com.sentinel.bridge.feature.notification.SentinelNotificationListener"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <service
            android:name="com.sentinel.bridge.feature.accessibility.SentinelAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <receiver
            android:name="com.sentinel.bridge.feature.pipeline.PipelineEntryReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.sentinel.bridge.START_PIPELINE" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
